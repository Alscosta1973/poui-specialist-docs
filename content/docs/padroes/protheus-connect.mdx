---
title: "Guia de Integração com Protheus"
description: "Como solicitar a conexão de um componente Angular aos dados reais do Protheus REST"
---

# Guia de Integração com Protheus Real

Use este guia quando quiser substituir os mocks de um componente Angular pela API REST real do Protheus.
Quanto mais completo o pedido, menos idas e vindas serão necessárias.

## Formato do pedido

Copie o template abaixo e preencha com os dados do seu componente:

```
Quero conectar <ComponentClass> ao Protheus real.

TABELAS:
  <alias>  (<nome>)  campos: <lista>

ENDPOINT:
  Existe: sim → URL: <url>  Método: GET|POST|DELETE
  Não existe → precisa criar TLPP

AUTH:
  Tipo: Basic|Bearer|sem auth
  Header: <valor ou "fixo no proxy">

PROTHEUS:
  URL base: http://<ip>:<porta>
  Prefixo REST: /rest/api/custom/v1

MOCK A REMOVER:
  <arquivo-interceptor>

AÇÕES NOVAS (se houver):
  POST <endpoint> com payload { <campos> }
```

---

## Campos explicados

### TABELAS

Liste os aliases Protheus e os campos que a tela consome.

```
SE1  (Títulos a Receber)  campos: E1_NUM, E1_PARCELA, E1_TIPO, E1_NOMCLI, E1_VALOR, E1_SALDO, E1_VENCTO
SA1  (Clientes)           campos: A1_COD, A1_LOJA, A1_NOME, A1_CGC
SEA  (Mov. Cartão)        campos: EA_DTMOV, EA_CARTAO, EA_VALOR, EA_STATUS, EA_CODFOR
```

Se não souber os campos de cor, informe a rotina ADVPL que já acessa esses dados (ex: `MATA910`, `FIN010`) e o campo será extraído do código fonte.

### ENDPOINT

**Já existe no Protheus:**

```
URL:     /rest/api/custom/v1/financeiro/divergencias
Método:  GET
Params:  filial, dataInicio, dataFim, status
Response (exemplo):
  {
    "items": [
      { "E1_NUM": "000001", "E1_VALOR": 1500.00, "E1_VENCTO": "20260101" }
    ],
    "hasNext": false
  }
```

**Não existe — precisa criar TLPP:**

Informe as regras de negócio:

- O que define um registro como "divergência" (ex: saldo != 0 após conciliação)
- Quais filtros e ordenação o usuário pode aplicar
- Quais ações o usuário executa (aprovar, rejeitar, confirmar)

O comando `/poui-specialist:connect` gera automaticamente o skeleton TLPP com os métodos WsRestFul.

### AUTH

| Tipo | Como informar |
|------|--------------|
| Sem autenticação | `AUTH: sem auth` |
| Basic Auth | `AUTH: Basic — usuário admin / senha fixa no proxy` |
| Bearer Token | `AUTH: Bearer — token vem do localStorage['token']` |
| Cookie de sessão | `AUTH: Cookie — Protheus mantém sessão após login` |

> **Segurança:** credenciais no `proxy.conf.json` são apenas para desenvolvimento local.
> O arquivo é adicionado ao `.gitignore` automaticamente pelo plugin quando contém senhas.

### PROTHEUS — URL base

```
URL base:     http://192.168.1.10:8086
Prefixo REST: /rest/api/custom/v1
```

O plugin cria ou atualiza o `proxy.conf.json` para redirecionar `/rest` → Protheus:

```json
{
  "/rest": {
    "target": "http://192.168.1.10:8086",
    "secure": false,
    "changeOrigin": true,
    "logLevel": "info"
  }
}
```

### MOCK A REMOVER

Informe o arquivo do interceptor (ou "localizar automaticamente" — o plugin busca):

```
MOCK A REMOVER: src/app/core/interceptors/mock-divergencias.interceptor.ts
```

O plugin desativa o interceptor removendo-o do `app.config.ts`. O arquivo pode ser mantido comentado para rollback fácil.

### AÇÕES NOVAS

Para cada botão que vai acionar o backend além do GET de lista:

```
AÇÕES NOVAS:
  POST /rest/api/custom/v1/financeiro/divergencias/confirmar
    Payload: { ids: string[] }
    Response: { sucesso: number, falha: number, itens: [...] }

  DELETE /rest/api/custom/v1/financeiro/divergencias/:id
    Response: 204 No Content
```

---

## Exemplo completo de pedido

```
Quero conectar DivergenciasCartaoComponent ao Protheus real.

TABELAS:
  SE1 (Títulos a Receber): E1_NUM, E1_PARCELA, E1_TIPO, E1_NOMCLI, E1_VALOR,
                            E1_SALDO, E1_VENCTO, E1_SITUACA
  SEA (Movimentos Cartão): EA_DTMOV, EA_CARTAO, EA_VALOR, EA_STATUS, EA_CODFOR

ENDPOINT:
  Existe: sim
  URL: /rest/api/custom/v1/financeiro/divergencias
  Método: GET
  Params: filial, dataInicio, dataFim
  Response: { items: [...], hasNext: bool }

AUTH:
  Basic Auth — usuário "admin" / senha fixa no proxy

PROTHEUS:
  URL base: http://192.168.1.10:8086
  Prefixo REST: /rest/api/custom/v1

MOCK A REMOVER:
  src/app/core/interceptors/mock-divergencias-cartao.interceptor.ts

AÇÕES NOVAS:
  POST /financeiro/divergencias/confirmar  payload: { ids: string[] }
  POST /financeiro/divergencias/rejeitar   payload: { id: string, motivo: string }
```

---

## O que o plugin faz com essas informações

1. Lê o service e component existentes
2. Identifica padrões mock (`of(MOCK_*)`, `delay()`, constantes `DEMO_*`)
3. Atualiza `proxy.conf.json` com o target do Protheus
4. Substitui o service: `of(data)` → `this.http.get(...)`
5. Remove ou desativa o interceptor de mock
6. Gera o contrato TLPP se o endpoint não existir
7. Atualiza os specs: troca mocks fixos por `HttpTestingController`
8. Roda build + testes para confirmar que nada quebrou

---

## Relacionado

- [`/poui-specialist:connect`](/docs/comandos/connect) — o comando que executa a integração
- [Padrões REST Protheus](/docs/padroes/protheus-rest) — contratos de request/response esperados
- [Deploy Protheus](/docs/padroes/deploy-protheus) — como compilar e publicar o TLPP no RPO
